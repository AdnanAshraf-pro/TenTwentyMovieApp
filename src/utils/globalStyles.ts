import { StyleSheet, Text, View } from 'react-native';
import { PrimaryBackground } from './constant';

const GlobalStyles = StyleSheet.create({
  mainWrapper: {
    flex: 1,
    backgroundColor: PrimaryBackground,
  },
});

export default GlobalStyles